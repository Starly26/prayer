import React from 'react';
import {SafeAreaView} from 'react-native-safe-area-context';
import {Text} from 'react-native-svg';

const MyPrayers = () => {
  return (
    <SafeAreaView>
      <Text>MyPrayerrs</Text>
    </SafeAreaView>
  );
};

export default MyPrayers;
